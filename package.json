{
  "name": "@micro-js/supports-gen",
  "description": "Supports generator syntax.",
  "repository": "git://github.com/micro-js/supports-gen.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/evaluates": "^1.0.0"
  },
  "devDependencies": {
    "semver": "^5.1.0",
    "tape": "^4.2.2"
  }
}
